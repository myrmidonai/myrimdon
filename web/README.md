# web (placeholder)

Vite + React SPA — the visual workflow editor, chat/IM surface, and review queue.
A **thin client over the Connect/gRPC network protocol** (PRD6 §8/§9/§17); embedded in
the desktop shell (`/desktop`). **Not Next.js** (SSR/Node server conflicts with embedding).

Arrives in **M2/M3** (PRD6 §21). TS types are codegen'd from `/schema` (single contract source).
