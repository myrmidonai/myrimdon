# sdk (placeholder)

TypeScript SDK — an optional code-authoring path that emits `WorkflowDef` JSON
(the engine only ever consumes JSON; this is sugar, not a runtime dependency).
Types are codegen'd from `/schema`. Distributed via npm (`npx`).

Optional / later milestone (PRD6 §4, §8). JSON/YAML + visual editor + AI generation
are the primary authoring paths.
